<%= render_session_component("front/topics/Show", {
  user: {
    username: @topic.user.username
  },
  repo: @topic.repo,
  title: @topic.title,
  content: render_markdown(@topic.content),
  label_text: @topic.label_text,
  created_at: render_time(@topic.created_at)
}) %>
